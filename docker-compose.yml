services:

  mongodb:
    image: mongo:latest
    container_name: notes-mongodb

    ports:
      - "27017:27017"

    volumes:
      - mongo-data:/data/db

  backend:
    image: karthickroshan77/notes-backend:latest

    container_name: notes-backend

    ports:
      - "5000:5000"

    environment:
      MONGO_URL: mongodb://mongodb:27017/notesdb

    depends_on:
      - mongodb

  frontend:
    image: karthickroshan77/notes-frontend:latest

    container_name: notes-frontend

    ports:
      - "5173:5173"

    depends_on:
      - backend

volumes:
  mongo-data: